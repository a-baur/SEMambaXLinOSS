# S4D -- inherits shared config from base.yaml; only the mixer block differs.
base: ../base.yaml

model_cfg:
  norm_epsilon: 1.0e-05
  causal_conv: true
  ssm: s4d
  d_state: 128
  d_conv: 4
  expand: 2
  ssm_params:
    dt_min: 0.001
    dt_max: 0.1
